# Copy to .env.local for local development
NEXT_PUBLIC_API_URL=http://localhost:8000
# Must match CALL_CENTER_API_KEY in the backend .env (NOT SECRET_KEY)
NEXT_PUBLIC_DEFAULT_API_KEY=dev-local-api-key
